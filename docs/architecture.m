# Architecture

Manthan consists of:

- Schema → defines input
- Rules → defines logic
- Core → evaluates
- Operator → executes

---

## Principle

Core is domain-agnostic.

All domain logic exists in:
- schema
- rules
- signals